// apps/api is a route-handler-only backend; this placeholder page exists
// only so Next.js has a valid root segment to render (required for its
// internal 404/error page prerendering). No UI belongs here — see
// apps/consumer, apps/estabelecimento and apps/backoffice for the frontends.
export default function ApiRoot() {
  return <p>marka.ia API</p>;
}
